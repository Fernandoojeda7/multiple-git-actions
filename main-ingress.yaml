apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: main-ingress
spec:
  rules:
    - host: http://34.38.80.162/
      http:
        paths:
          - pathType: Prefix
            path: /
            backend:
              service:
                name: mequipzilla-main
                port:
                  number: 80
